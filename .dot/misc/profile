# used by sh
# also used by LightDM; which otherwise doesn't get any environment from shell etc.
export RAN_PROFILE=TRUE
export DFG_FILES_SOURCED="${DFG_FILES_SOURCED}profile,"
[[ -f "${HOME}/.dot/l/shrc/both" ]] && source "${HOME}/.dot/l/shrc/both"
